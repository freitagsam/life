# Life Test

A minimal Expo SDK 57 app for testing the GitHub → Codex → Expo Go workflow.

## Run it

```bash
npm install
npx expo start --tunnel
```

Open the resulting Expo link or QR code with Expo Go.

## What it does

The first screen confirms that the app loaded and includes a working button counter.
